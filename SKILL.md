---
name: storyboard-generator
description: GPT Image 2 优先的 AIMV/MV 分镜脚本生成器。当用户需要把歌词、LRC、歌曲时长或主题设定转成可生产的 MV 故事板方案时使用：先锁定角色/场景/道具资产，再生成多宫格 storyboard sheet prompt、必要单镜关键帧 prompt、视频运动 prompt、剪辑点和小白生产自检。触发词：写分镜、分镜脚本、MV storyboard、故事板、生图分镜、AI视频分镜。
---

# GPT Image 2 AIMV 分镜脚本生成器

## 目标

生成可直接进入生图、图生视频和剪辑的 MV 分镜方案。默认以 GPT Image 2 的强项组织输出：先做一致性资产锚定图，再做多宫格故事板图，最后只为关键镜头补单帧或尾帧。

不要把本技能做成教程。输出要像生产清单，让不懂分镜的用户知道先生成哪张图、哪几张图可以裁切成分镜图、哪些镜头才需要额外补图。

## 核心原则

- **故事优先**：先有完整故事弧线，再切分镜头。歌词是情绪和节奏依据，不是一句歌词一张图。
- **GPT Image 2 优先**：默认使用“资产参考图 -> 多宫格故事板 sheet -> 单镜补图/尾帧”的生产链路，而不是每个分镜都单独文生图。
- **少即是稳**：能用同一张故事板 sheet 覆盖的连续镜头，不要拆成多条孤立 prompt。
- **结束帧降级**：不要在主表里强制给每个镜头写结束帧 Prompt。只有动作终点、情绪反转、空间变化、视频模型必须首尾帧时，才输出关键尾帧。
- **生产可执行**：所有 prompt 都描述可见物理画面，避免“孤独、释然、遗憾”这类不可直接渲染的抽象词单独出现。
- **小白可用**：表格再专业，也必须让用户知道“这张图拿去生图”“这几格裁切”“这个镜头再补一张”。

## 输入处理

支持三档输入，不因缺少完整信息而停止：

| 模式 | 输入条件 | 处理方式 |
|------|----------|----------|
| 精准模式 | 有 LRC 时间轴 | 按时间戳计算歌词行时长、纯音乐段和镜头时间码 |
| 校准模式 | 有歌词和总时长 | 按段落权重估算后强制校准到歌曲总时长 |
| 草案模式 | 只有歌词或主题 | 生成可讨论草案，明确标注“时间码为估算值” |

如果用户未提供视觉风格，不要阻塞；先给一版“可替换默认风格”，并在输出开头标注。只有当用户明确要求高精度定稿且缺少风格时，才先追问。

## 工作流

### Step 1：歌曲结构与故事弧线

先输出简短分析：

- 歌曲结构：Intro / Verse / Chorus / Bridge / Outro，包含纯音乐段。
- 情绪曲线：起点、压抑点、爆发点、回落点。
- 核心故事：什么人在什么处境下经历了什么。
- 幕次划分：通常 3-5 幕，每幕有明确戏剧任务。
- 镜头节奏：抒情段 5-7 秒/镜，常规叙事 4-6 秒/镜，高潮 3-5 秒/镜。

不要把“失恋、遗憾、成长”停留在概念层面，必须落到可拍的动作、空间和道具变化。

### Step 2：资产锁定

在分镜表之前必须输出资产锁定表。它是后续 GPT Image 2 参考图和故事板一致性的依据。

```markdown
## 资产锁定表

| 类型 | 名称 | 固定设定 | 可变化部分 | 对应资产图 | 使用范围 |
|------|------|----------|------------|----------|----------|
| 角色 | 男主 | 28岁，瘦削，黑色短发，银框眼镜，深灰衬衫，黑色长外套 | 表情、湿度、疲惫程度 | A1 | 全片 |
| 场景 | 深夜公寓 | 高层公寓卧室，窗外冷蓝城市夜景，电脑桌，黑色台灯 | 屏幕亮度、窗外雨势、桌面凌乱程度 | A2 | Intro / Verse 1 |
| 道具 | 智能手机 | 黑色手机，屏幕冷光，聊天界面只表现为模糊色块 | 亮屏/锁屏、位置、反光 | A3 | 1-6镜 |
```

规则：

- 角色必须锁定年龄段、发型、脸型/气质、服装、随身物。
- 场景必须锁定空间功能、主要家具/物件、光线和色调。
- 道具必须锁定外观、材质、状态变化和出现范围。
- 资产锁定表只负责定义设定和验收标准，不作为用户复制生图的入口；用户不应该手动把固定设定复制到 Prompt 里拼接。
- `对应资产图` 可以是一对一，也可以是合并生成：例如多个小道具可共同对应 `A6 道具 contact sheet`；低优先级背景角色可标记为“随 A5/SB-09 出现，不单独生成”。
- GPT Image 2 资产图生成清单必须有 `覆盖锁定项` 列，说明每张资产图覆盖资产锁定表里的哪些行，避免用户误以为漏了角色、场景或道具。
- 资产图编号必须和后续故事板引用一致；如果新增一个空间资产，后续 `参考资产`、`故事板Prompt`、生产顺序都要同步改编号。
- 如果用户未给设定，自动生成一版“可替换设定”，不要让角色和道具在镜头间漂移。

### Step 3：GPT Image 2 资产图生成清单

先生成资产图，再生成故事板。资产图不是可选项，除非用户明确说只要文字分镜。

资产图生成清单才是实际生图入口。`可直接复制Prompt` 必须是自包含的完整 prompt，已经内嵌对应锁定项的固定设定；用户复制该列即可生图，不需要再从资产锁定表手动拼接。

```markdown
## GPT Image 2 资产图生成清单

| 编号 | 类型 | 覆盖锁定项 | 目的 | 建议尺寸 | 可直接复制Prompt | 使用方式 |
|------|------|------------|------|----------|--------|----------|
| A1 | 角色锚定图 | 男主 | 锁定男主脸、发型、服装、体态 | 1536x1024 | ... | 后续所有男主镜头作为参考图 |
| A2 | 场景锚定图 | 深夜公寓 | 锁定深夜公寓空间关系和冷蓝光线 | 2048x1152 | ... | SB-01/SB-02 故事板参考 |
```

资产图 Prompt 写法：

- 每条资产图 Prompt 必须完整包含它覆盖的固定设定，不能要求用户从资产锁定表复制补全。
- 首轮资产图 Prompt 不要写“同一位”“来自 A1”“same as reference”这类依赖既有参考的措辞，除非用户已经提供参考图。首轮应写“创建一名角色/一个空间/一组道具”；同一张 contact sheet 内只需要说明“各视角保持脸型、发型、服装一致”。
- 不要为了少生成一张图而把视觉差异很大的空间硬塞进同一张资产图。咖啡店/玻璃走廊可以合并为城市社交空间；雨夜公交/居民楼下/伞下街口应拆成另一张回忆夜路空间；婚礼现场再单独一张。
- 只有小道具、同一空间的几个角度适合合并成 contact sheet；主要角色不要默认塞进普通四宫格 contact sheet。
- 角色锚定图优先使用 `character design reference board / character turnaround sheet`，固定版式必须写清楚：左侧 35%-40% 放一张清晰胸像或半身肖像，正脸、眼神和气质可见；右侧 60%-65% 放同一角色的全身三视图，依次为正面、侧面、背面，脚部完整，身高比例一致，服装一致，纯背景或浅灰背景，干净分隔线，无字幕、无标签、无水印。
- 角色定版图遵守“一套造型一张图”。如果角色在日常段和婚礼段有明显换装，不要塞进同一张资产图；应拆成“日常定版图”和“婚礼定版图”。后生成的换装定版图可以引用前一张角色脸部资产，但 Prompt 仍要完整写出脸型、发型、气质和新服装，避免用户手动拼接。
- 场景锚定图优先使用宽幅环境图：固定空间、主要物件位置、光源方向、色调。
- 道具锚定图只在道具会反复出现或推动剧情时单独生成。
- Prompt 应说明“无字幕、无水印、无屏幕可读文字”，除非该图本来就是标题卡或海报。
- 如果用户已有角色图/场景图，要求后续 prompt 使用“参考图中的同一角色/同一空间”，不要重新发明设定。

### Step 4：切分镜头

按故事弧线和音乐结构切镜头：

- 连续同场景动作优先合并到同一故事板 sheet。
- 情绪高潮可以镜头更密，但不要出现大量 1-2 秒不可生产镜头。
- 纯音乐段必须有画面任务，不能只写“空镜”。
- 每个镜头必须有“画面目标”，例如建立空间、介绍状态、抛出道具、推进行动、制造反差、情绪爆发、留白、完成告别。

### Step 5：多宫格故事板分组

这是 GPT Image 2 优先版本的核心输出。每 4-6 个连续镜头生成一张故事板 sheet。4 镜用 2x2；5-6 镜用 2x3；不要把超过 6 个镜头塞进一张图。

```markdown
## GPT Image 2 故事板生成表

| 故事板编号 | 包含镜头 | 段落 | 宫格 | 故事板目标 | 参考资产 | 建议尺寸 | 故事板Prompt | 输出用途 |
|------------|----------|------|------|------------|----------|----------|----------------|----------|
| SB-01 | 1-5 | Intro / Verse 1 | 2x3 | 建立深夜公寓、手机和男主压抑状态 | A1, A2, A3 | 2048x1152 | ... | 生成后裁切第1-5格作为分镜图 |
```

故事板 Prompt 必须遵守：

- 明确写成 `multi-panel cinematic storyboard sheet` 或中文等价表达。
- 写清宫格布局、干净分隔线、每格独立可裁切。
- 使用资产图中的同一角色、同一场景、同一道具。
- 每格只描述关键可见状态，不把歌词解释塞进去。
- 每格必须是一个静态关键状态，不写“随后、然后、从A到B、tight then loosening”这类时间过程；动作过程和终点放到视频运动 Prompt 或关键尾帧清单。
- 不依赖模型在图内写镜号、字幕或中文标签；镜号由表格记录。
- 同一张 sheet 内尽量保持同一视觉风格、光线逻辑和人物身份。
- 发生大空间跳跃、年代跳跃或主情绪突变时，另起一张 sheet。

推荐故事板 Prompt 结构：

```text
[全局风格]，multi-panel cinematic storyboard sheet，2x3 grid，clean white gutters，each panel is a separate crop-ready frame，no captions，no watermark。
Use the same male lead from reference image A1, the same apartment from reference image A2, and the same black smartphone from reference image A3.
Panel 1: ...
Panel 2: ...
Panel 3: ...
...
保持同一角色脸型、发型、服装和冷蓝夜景光线，电影级写实摄影，高细节。
```

### Step 6：分镜执行表

主表不再强制输出“结束帧Prompt”。主表负责让用户知道每个镜头如何从故事板或补图进入视频制作。

```markdown
## 分镜执行表

| 镜号 | 时间码 | 秒 | 所属故事板 | 画面目标 | 画面描述 | 关键帧来源 | 单帧补图Prompt | 视频运动Prompt | 尾帧策略 | 转场/剪辑点 | 景别 | 歌词 | 情绪 |
|------|--------|----|------------|----------|----------|------------|----------------|-----------------|----------|--------------|------|------|------|
```

字段规则：

- **所属故事板**：填写 SB-01 第几格；如果需要单独补图，仍标注关联故事板。
- **关键帧来源**：使用 `故事板裁切`、`参考故事板重绘`、`单帧补图`、`首尾帧精控` 四类。
- **单帧补图Prompt**：大多数镜头写“使用 SB-XX 第X格裁切/参考重绘”。只有需要更高清、更准确构图或独立封面感时，才写完整单帧 Prompt。
- **视频运动Prompt**：描述动作、运镜、速度、幅度和禁止漂移项；不新增画面中没有的角色或道具。
- **尾帧策略**：使用 `不需要`、`建议`、`必须`。只有 `必须` 或少量 `建议` 镜头进入关键尾帧清单。

### Step 7：关键尾帧清单

只为真正需要首尾帧控制的镜头输出，不要全片铺满。

```markdown
## 关键尾帧清单

| 镜号 | 需要尾帧的原因 | 起始参考 | 结束帧Prompt | 视频用途 |
|------|----------------|----------|--------------|----------|
| 6 | 手机从手中放回桌面，动作终点会影响剪辑 | SB-01 第6格 | ... | 用作图生视频尾帧 |
```

需要尾帧的典型情况：

- 人物或道具位置有明确终点。
- 光线、天气、时间发生可见变化。
- 需要从一个情绪状态过渡到另一个情绪状态。
- 视频工具对动作稳定性要求高，单图运动容易漂移。
- 下一镜头依赖这个终点做匹配剪辑。

不需要尾帧的情况：

- 轻微推拉摇移。
- 静态情绪镜头。
- 只需要呼吸感、雨滴、屏幕微光等细小运动。

### Step 8：视频制作清单

把分镜脚本翻译成剪辑执行顺序。

```markdown
## 视频制作清单

| 视频段 | 使用图片 | 时长 | 视频运动Prompt | 转场/剪辑 |
|--------|----------|------|-----------------|-----------|
| V01 | SB-01 第1格裁切 | 6s | 固定机位，窗外城市灯光轻微闪烁，手机屏幕冷光稳定 | 淡入，接硬切 |
```

这张表要告诉用户：哪张图进视频工具、持续几秒、怎么动、怎么接下一镜头。

## Prompt 写作规则

### 静态图 Prompt

使用五段式：

```text
[风格] -> [场景/环境] -> [主体+固定特征] -> [关键细节] -> [构图/光线/画质]
```

规则：

- 优先继承资产锁定表，尤其是角色年龄、发型、服装、核心道具。
- 每条 prompt 聚焦 3-5 个关键视觉信息，不堆十几个细节。
- 用具体摄影词：35mm、50mm、85mm、浅景深、逆光、冷蓝窗光、暖色台灯。
- 把抽象情绪改成可见状态：肩膀收紧、眼神避开、手指停在屏幕上、杯子边缘水痕。
- 风格从正面写，不靠大量负面词控制。
- 技术性负面词最多 2-3 个：模糊、畸形手指、重复面孔。

### 故事板 Prompt

- 每格画面必须可单独裁切成分镜图。
- 不在图内生成字幕、歌词、镜号或中文说明。
- 如果必须有可读文字，单独输出“文字图/标题卡 Prompt”，不要混进普通故事板。
- 同一 sheet 内的角色脸、服装、场景关系必须一致。
- 可以使用一张故事板作为下一张故事板的参考图，延续角色和光线。

### 视频运动 Prompt

使用结构：

```text
[人物/物体动作] + [镜头运动] + [速度/幅度] + [保持一致项] + [禁止漂移项]
```

示例：

```text
男主手指停在手机屏幕上后慢慢松开，镜头轻微推近，屏幕冷光保持稳定，桌面道具位置不变，避免新增文字和人物。
```

## 输出格式

生产版必须按以下顺序输出：

1. `## 《歌曲名》GPT Image 2 分镜方案`
2. `## 输入完整度与模式判定`
3. `## 视觉风格与故事弧线`
4. `## 资产锁定表`
5. `## GPT Image 2 资产图生成清单`
6. `## GPT Image 2 故事板生成表`
7. `## 分镜执行表`
8. `## 关键尾帧清单`
9. `## 视频制作清单`
10. `## Prompt 汇总`
11. `## 小白生产自检`

如果用户要求“简单版/默认模式”，仍保留前 7 项，但 `Prompt 汇总` 可以只列故事板 sheet 和需要补图的镜头。

## 小白生产自检

必须诚实输出，不允许全部写通过。

```markdown
## 小白生产自检

| 检查项 | 结果 | 说明 | 建议 |
|--------|------|------|------|
| 输入完整度 | 通过/可用但需注意/需补充 | 是否有 LRC、总时长、视觉风格 | 缺什么会影响什么 |
| 故事板可用性 | 通过/可用但需注意/需调整 | 每张 sheet 是否 4-6 格、是否可裁切 | 需要拆分或补图的位置 |
| 资产一致性 | 通过/可用但需注意/需调整 | 角色、场景、道具是否锁定 | 后续生图参考哪些资产 |
| 尾帧克制 | 通过/可用但需注意/需调整 | 是否只给必要镜头写尾帧 | 哪些尾帧可以删除 |
| 视频可执行性 | 通过/可用但需注意/需调整 | 每镜是否知道用哪张图和怎么动 | 哪些镜头需要重绘 |
| 剪辑可执行性 | 通过/可用但需注意/需调整 | 转场是否有节拍、动作或构图依据 | 哪些连接会突兀 |
```

自检必须至少指出 1-3 个可优化点，除非用户输入已经非常完整且要求只给最终定稿。

## 参考文档

- `references/storyboard-guide.md`：需要更深入的镜头语言、景别、运镜术语时读取。
- `references/prompt-engineering.md`：需要更细的中文文生图 prompt 优化技巧时读取。

只在需要时读取参考文档，不要为了普通分镜任务加载全部资料。
