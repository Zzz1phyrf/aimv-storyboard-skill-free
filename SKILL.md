---
name: storyboard-generator-free
description: AI视频分镜脚本生成器（免费版）。当用户需要为歌曲/音乐视频(AIMV)生成分镜脚本时使用。按场景切分，支持运镜组概念（首帧+尾帧配对生成视频）。输出结构化分镜表，包含中文文生图Prompt。触发词：写分镜、分镜脚本、分镜表、storyboard、做分镜、AI视频分镜。
agent_created: true
---

# AI视频分镜脚本生成器（免费版）

## 概述

根据歌曲歌词，生成完整的AI视频分镜脚本。核心原则：**按场景切分，不按歌词行切分**——同场景内的多句歌词合并到一个镜头，用运镜组（首帧+尾帧）生成动态视频片段。输出结构化表格，包含可直接用于文生图工具（ChatGPT image generation、NanoBanana、zimage等）的中文Prompt。

## 触发场景

- "帮我给这首歌写分镜"
- "生成一个AIMV分镜脚本"
- "做分镜表"
- "storyboard for this song"
- 用户提供歌词+主题描述时

## 核心工作流

### Step 0：确认全局信息（必须，不可跳过）

在生成分镜前必须向用户确认：
- **画风**：写实/动画/插画等
- **色调**：冷/暖/中性？
- **视觉参考**（可选）：有没有参考导演、电影或摄影师风格？
- **歌曲总时长**：必填，用于校验时长。

如果用户未指定，主动询问，绝不默认。

### Step 1：接收并解析输入

从用户获取以下信息：
- **歌词**：完整歌词文本。LRC带时间轴优先使用，没有则按行估算时间。
- **歌曲结构标注**（如歌词中有）：Intro / Verse / Chorus / Bridge / Outro
- **主题与情绪**：歌曲核心叙事、情感基调

### Step 2：分析歌曲结构

识别并标注歌曲的情绪曲线：
- **Intro**：铺垫氛围，建立视觉基调
- **Verse**：叙事段落，推进故事发展
- **Chorus**：情绪高点，视觉冲击力增强
- **Bridge**：情绪转折或回顾
- **Outro**：收束与释然

### Step 3：设计故事弧线

- 提炼核心故事（什么人、什么处境、经历了什么）
- 划分3-5幕，每幕有戏剧任务
- 每幕内把同场景的歌词合并，而不是逐行拆
- 确定情绪峰值位置（通常对应Chorus）

### Step 4：切分镜头（按场景，不按歌词行）

- 同一场景/同一动作 = 一个镜头，可以覆盖2-4句歌词
- 抒情慢歌每镜6-8秒，快歌每镜3-5秒
- 目标是全片约1张图/5-6秒（一张图首尾帧生成一段视频）
- **纯音乐段落**：用【纯音乐 时间范围 段落名】标注

### Step 5：生成分镜表

对每个镜头生成以下字段。

| 字段 | 说明 |
|------|------|
| 图号 | 从1递增。每个生图prompt一个编号。 |
| 时间点 | 该图对应的歌曲时间（mm:ss），不是时长范围。 |
| 运镜组 | 【M编号+运镜方式+时间范围+首帧/中间帧/尾帧】或【固定】。相邻的首帧和尾帧配对生成运镜视频。 |
| 画面描述 | 给人看的场景说明。可以有适度叙事表达。 |
| Prompt | 给AI文生图工具用的。纯物理可描述，不用比喻拟人。全局风格统一，每镜Prompt不附加各自风格标签。 |
| 景别 | 大远景/远景/全景/中景/近景/特写/大特写 |
| 歌词 | 对应歌词行（纯音乐段落标注时间范围） |

#### Prompt写作规范

**五段式结构**：
```
[场景环境] → [主体+物理特征] → [关键细节] → [光线+构图] → [色调]
```

**正面描述，不负面排除**：
- 想要写实：描述"自然光照明，浅景深，皮肤纹理可见"
- 不要写"禁止动画、禁止CGI、禁止手绘"
- 以后换风格（末日/动画），正面描述改了就行

**负面Prompt独立列出**：仅排除技术瑕疵 `模糊、变形的手指`（最多2-3个词），不和Prompt混在一起。

**避免的写法**：
- ❌ 高楼窗灯如孤岛、雨水似泪痕 → ✅ 居民楼亮着零星暖黄窗灯，雨水从玻璃上流下
- ❌ 身体后倾防御 → ✅ 身体微微后倾，肩膀收紧
- ❌ 数十栋高楼、所有建筑、约七八扇 → ✅ 几栋居民楼，零星几扇窗（不堆砌数字）

### Step 6：附加输出

分镜表之后输出：

1. **角色资产图Prompt**（如果有固定角色）：男女主各1张横版三视图——左侧半身肖像 + 右侧全身正面/侧面/背面。角色描述包含：发型、脸型、眼型、肤色、服装。先跑这2张锚定长相，再跑分镜图。

2. **运镜组汇总表**：列出所有运镜组——首帧图号、尾帧图号、运镜方式、时间范围、生成方式（首尾帧时长）

3. **技术性负面Prompt**：`模糊、变形的手指`

4. **统一视觉风格描述**：一行文字概括全片视觉调性，不在每镜prompt中重复。

### Step 7：时长校验

生成分镜表后，校验：
- 所有运镜组+固定镜头总时长是否等于歌曲总时长
- 是否存在超过10秒或少于2秒的极端镜头
- 如不满足，调整后重新输出

## 输出格式

### 主表：Markdown表格

```markdown
## 《歌曲名》分镜脚本

**歌曲时长**：X分X秒 | **总图数**：X张 | **视觉风格**：XXX

| 图号 | 时间点 | 运镜组 | 画面描述 | Prompt | 景别 | 歌词 |
|------|--------|--------|----------|--------|------|------|
| 1 | 00:00 | 【M1推镜 00:00-00:13 首帧】 | ... | ... | 大远景 | 【纯音乐】 |
```

### 运镜组汇总

```markdown
## 运镜组汇总

| 运镜组 | 时间 | 运镜 | 首帧图 | 尾帧图 | 生成 |
|--------|------|------|--------|--------|------|
| M1 | 00:00-00:13 | 推镜 | 图1(大远景) | 图3(中景) | 图1→2→3 三段首尾帧拼接13s |
```

### Prompt汇总

```markdown
## 分镜Prompt汇总（可直接复制生图）

#1 | 时间点 00:00 | 运镜: 推镜首帧
夜晚雨后居民区全景俯拍。几栋老式居民楼亮着零星暖黄窗灯。路灯照亮湿润柏油路面。冷蓝灰调，电影级写实摄影。

**技术性负面Prompt**：模糊、变形的手指
```

## 参考文档

- `references/storyboard-guide.md`：分镜设计原则、景别/运镜术语参考
- `references/prompt-engineering.md`：中文文生图Prompt优化技巧

当需要更深入的镜头语言知识或Prompt优化技巧时，读取以上参考文件。
